#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

vector <ll> d;

int main() {
#ifdef HOME
    freopen("input.txt", "rt", stdin);
#else
    freopen("part2num.in", "rt", stdin);
    freopen("part2num.out", "wt", stdout);
#endif
    ll x, sum = 0;
    while (cin >> x) {
        char c;
        cin >> c;
        sum += x;
        d.push_back(x);
    }
    ll cnt = 0;
    ll n = d;

    for (int i = 0; i < n; i++) {
        for (int j = 1; j < a[i] - 1; j++) {
            if ()
        }
    }
    return 0;
}
