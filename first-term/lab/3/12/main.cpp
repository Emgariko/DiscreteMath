#include <bits/stdc++.h>

using namespace std;

vector <int> d;
vector <unordered_set<int>> a;

void gen(int n, int k, int l) {
    if (l == n) {

    } else {
        for (int )
    }
}

int main() {
#ifdef HOME
    freopen("input.txt", "rt", stdin);
#else
    freopen("part2sets.in", "rt", stdin);
    freopen("part2sets.out", "wt", stdout);
#endif
    int n, k;
    cin >> n >> k;
    a.resize(k);
    d.resize(n);

    return 0;
}